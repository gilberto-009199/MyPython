# -*- coding: utf-8 -*-

funcionario        = str(input());
salarioFuncionario = float(input());
totalMes           = float(input());

print("TOTAL = R$ %.2f" % (salarioFuncionario + totalMes * 0.15));
